# 这里是线性回归
## 解析解是怎么搞出来的
* 首先要懂,你要干的是对loss function的优化是对着**w**求的导,因为X,y都是被固定了,也就是说是**选择一个最好的w**,自然就是对着w求导搞出来的驻点了.
* 具体怎么算的,你要理解这个平方的本质是$X^TX$,这样你就知道是
$$\mathbf{w}^* = (\mathbf X^\top \mathbf X)^{-1}\mathbf X^\top \mathbf{y}.$$的式子了,还算是好推
## 然后是随机梯度下降
我靠这个实在是太典了就不多说了,已经很熟悉了
## 有关矢量化加速
* 为什么
  * 因为真的快很多😋
## 正态分布和平方损失
* 公式如下: $$p(x) = \frac{1}{\sqrt{2 \pi \sigma^2}} \exp\left(-\frac{1}{2 \sigma^2} (x - \mu)^2\right).$$
* 然后是认为噪音服从正态分布后,给定了x的值,多出来的就是噪音,以及正态分布加上一个值只是平移,最后就能推出这个似然估计的式子:
$$P(y \mid \mathbf{x}) = \frac{1}{\sqrt{2 \pi \sigma^2}} \exp\left(-\frac{1}{2 \sigma^2} (y - \mathbf{w}^\top \mathbf{x} - b)^2\right).$$
* 那么对于整个数据集:
 $$P(\mathbf y \mid \mathbf X) = \prod_{i=1}^{n} p(y^{(i)}|\mathbf{x}^{(i)}).$$
* 根据极大似然估计法,再做个负对数,就可以得到:
  $$-\log P(\mathbf y \mid \mathbf X) = \sum_{i=1}^n \frac{1}{2} \log(2 \pi \sigma^2) + \frac{1}{2 \sigma^2} \left(y^{(i)} - \mathbf{w}^\top \mathbf{x}^{(i)} - b\right)^2.$$
  发现这个就是均方误差 ~~(我去,这不是我读SVD时候忽略的推论吗)~~
## 过渡形式的,什么是神经网络
* 对于线性回归，每个输入都与每个输出（在本例中只有一个输出）相连， 我们将这种变换（ 图3.1.2中的输出层） 称为全连接层（fully-connected layer）或称为稠密层（dense layer）。
## 练习题不是很友好啊
1. 1. 就对着式子求一下b的偏导,算一下发现驻点是$b=E(x)$
   2. 一样的,写出正态分布误差的式子,依旧是最小化一个均方误差,那么用1.1的结论,我是$b=E(x)$是最小负对数似然
2. 1-3都略了,很显然的内容
   4. 解析解主要的复杂度应该是矩阵乘法求$(X^TX)^{-1}$,这直接是$O(d^3)$的复杂度,所以说在d很大的时候,请用梯度下降
3. 1. 我草直接爆算!
$$
-\log P(\mathbf y\mid X;\mathbf w)
=
-\sum_{i=1}^n
\log
\left[
\frac12
\exp\left(
-\left|y_i-\mathbf x_i^\top\mathbf w\right|
\right)
\right]
$$
打开一下发现其实就是L1范数
    2. ~~这后面是说的GPT~~ 去掉绝对值符号,问题就可以转化为线性规划问题,但是没有解析解
    $$
\begin{aligned}
r_i &\ge y_i - \mathbf{x}_i^\top \mathbf{w} \\
r_i &\ge -y_i + \mathbf{x}_i^\top \mathbf{w} \\
r_i &\ge 0
\end{aligned}
$$
    3. 用随机梯度下降的时候,会遇到$|r|$这个在r=0的时候不可导的问题,这导致随机梯度下降会在最优值的附近震荡
    4. 解决方法?特判一下r=0的时候梯度为0,或者用逐渐减小的学习率,以及用一种平滑的近似:
$$
L_\delta(r)=
\begin{cases}
\frac{1}{2}r^2, & |r|\le \delta \\
\delta\left(|r|-\frac{1}{2}\delta\right), & |r|>\delta
\end{cases}
$$
    它在残差较小时像平方损失，残差较大时像绝对值损失，因此既更平滑，也比纯平方损失更抗异常值。