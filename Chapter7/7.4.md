# Googlenet
## Inception块
![](\Inception块.png)
### 这么猎奇的块怎么搞出来的
#### ~~(来自GPT)~~ 核心思想很朴素:
  * 既然我不知道某一层该用多大的卷积核，那就 1×1、3×3、5×5、池化都试一遍，然后把结果拼起来。
  * 小卷积核适合看细节：边缘、纹理、小局部结构
  * 大卷积核适合看更大范围：局部部件、更大的形状组合
  * 池化适合保留比较强的响应：哪里有明显特征
  * 全部放在一起,让模型自己选择
#### 但是为什么又要加很多1*1的卷积核呢?
  * 先用 1×1 卷积把通道数从 192 降到 16，再做 5×5：
  * 直接 5×5 卷积参数量是：
    * 192 × 32 × 5 × 5 = 153,600
  * 但如果先用 1×1 卷积把通道数从 192 降到 16，再做 5×5：
    * 1×1 降维：192 × 16 = 3,072
    * 5×5 卷积：16 × 32 × 5 × 5 = 12,800
    * 总计：15,872
  * 差不多少了 90% 的参数。
## 好复杂的架构!
![](\googlenet.png)
* 这个最猎奇的位置在于每个串联inception层的通道数分配:
> 第三个模块串联两个完整的Inception块。 第一个Inception块的输出通道数为 64+128+32+32=256 ，四个路径之间的输出通道数量比为 64:128:32:32=2:4:1:1 。 第二个和第三个路径首先将输入通道的数量分别减少到 96/192=1/2 和 16/192=1/12 ，然后连接第二个卷积层。第二个Inception块的输出通道数增加到 128+192+96+64=480 ，四个路径之间的输出通道数量比为 128:192:96:64=4:6:3:2 。 第二条和第三条路径首先将输入通道的数量分别减少到 128/256=1/2 和 32/256=1/8 。

> 第四模块更加复杂， 它串联了5个Inception块，其输出通道数分别是 192+208+48+64=512 、 160+224+64+64=512 、 128+256+64+64=512 、 112+288+64+64=528 和 256+320+128+128=832 。 这些路径的通道数分配和第三模块中的类似，首先是含 3×3 卷积层的第二条路径输出最多通道，其次是仅含 1×1 卷积层的第一条路径，之后是含 5×5 卷积层的第三条路径和含 3×3 最大汇聚层的第四条路径。 其中第二、第三条路径都会先按比例减小通道数。 这些比例在各个Inception块中都略有不同。
* 基本上可以认为是作为超参一下一下调出来的
* 主要的思路就是为3*3的卷积核分配最多的通道数,因为这个的效果最平均啊,5*5的有点太贵了
## 自己试试然后出问题了....
![](\googlenet-fail.png)
> 这是典型的 训练发散：学习率太大，参数更新过猛，导致 loss 变成 nan，最后准确率也崩掉了。
感觉这个问题还是挺容易发生的,学习率高点就会这样感觉是,lr=0.2的时候有一样的现象