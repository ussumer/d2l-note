# 注意力汇聚：Nadaraya-Watson 核回归
## 非参数注意力汇聚
因为你要是就做个平均汇聚,出来的函数很简单:
$$f(x) = \frac{1}{n}\sum_{i=1}^n y_i,$$
显然，平均汇聚忽略了输入$x_i$,根据输入的位置对输出$y_i$进行加权：
$$f(x) = \sum_{i=1}^n \frac{K(x - x_i)}{\sum_{j=1}^n K(x - x_j)} y_i,$$
其中$K$是*核*（kernel）。
收到他的启发,我们可以搞一个更加通用的注意力汇聚公式:
$$f(x) = \sum_{i=1}^n \alpha(x, x_i) y_i,$$
为了更好地理解注意力汇聚，
下面考虑一个*高斯核*（Gaussian kernel），其定义为：

$$K(u) = \frac{1}{\sqrt{2\pi}} \exp(-\frac{u^2}{2}).$$

将高斯核代入 :eqref:`eq_attn-pooling`和
 :eqref:`eq_nadaraya-watson`可以得到：

$$\begin{aligned} f(x) &=\sum_{i=1}^n \alpha(x, x_i) y_i\\ &= \sum_{i=1}^n \frac{\exp\left(-\frac{1}{2}(x - x_i)^2\right)}{\sum_{j=1}^n \exp\left(-\frac{1}{2}(x - x_j)^2\right)} y_i \\&= \sum_{i=1}^n \mathrm{softmax}\left(-\frac{1}{2}(x - x_i)^2\right) y_i. \end{aligned}$$
:eqlabel:`eq_nadaraya-watson-gaussian`

在 :eqref:`eq_nadaraya-watson-gaussian`中，
如果一个键$x_i$越是接近给定的查询$x$，
那么分配给这个键对应值$y_i$的注意力权重就会越大，
也就“获得了更多的注意力”,
简单来说,某个$xi$如果离新加入的$x$很近,那么这个$y$,也应该离$yi$近
> 注意力汇聚是$y_i$的加权平均。
> 为了更好地理解注意力汇聚，
> 下面考虑一个*高斯核*（Gaussian kernel），其定义为：
> 
> $$K(u) = \frac{1}{\sqrt{2\pi}} \exp(-\frac{u^2}{2}).$$
> 
> 将高斯核代入 可以得到：
> 
> $$\begin{aligned} f(x) &=\sum_{i=1}^n \alpha(x, x_i) y_i\\ &= \sum_{i=1}^n \frac{\exp\left(-\frac{1}{2}(x - x_i)^2\right)}{\sum_{j=1}^n \exp\left(-\frac{1}{2}(x - x_j)^2\right)} y_i \\&= \sum_{i=1}^n \mathrm{softmax}\left(-\frac{1}{2}(x - x_i)^2\right) y_i. \end{aligned}$$
> 如果一个键$x_i$越是接近给定的查询$x$，
> 那么分配给这个键对应值$y_i$的注意力权重就会越大，
> 也就“获得了更多的注意力”。

**也就是在说:**
>注意力机制本质上可以看成“根据 query 和 key 的相似度，对 value 做加权平均”。在这个例子里，相似度由距离决定，离 query 越近的 key，对应 value 的权重越大。
![](\softmax.png)
![](\softmax_hot.png)
没那么好,但是最起码在干事
## 带参数注意力汇聚
### 简单介绍了一下批量矩阵乘法,
$torch.bmm(weights.unsqueeze(1), values.unsqueeze(-1))$
=>
$weights.unsqueeze(1) @ values.unsqueeze(-1)$
都是
>unsqueeze函数用于在张量的指定位置添加一个大小为1的新维度
$(2, 1, 10) @ (2, 10, 1) -> (2, 1, 1)$
### 代码是?
定义Nadaraya-Watson核回归的带参数版本为:
```py
 self.attention_weights = nn.functional.softmax(
            -((queries - keys) * self.w)**2 / 2, dim=1)
```
![](\one_para.png)
![](\one_para_hot.png)
还行吧
### 如何理解不平滑的热力图
热力图你就横着看,就是在说这个参数究竟是在看哪些参数的意义上跑出来的
也就是说,attention越集中的位置,越可能不平滑,也就是说越可能**过拟合**